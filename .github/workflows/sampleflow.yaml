# action.yml
name: 'Hello World'
on: [push]
jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v3
        with:
          node-version: '14'
          cache: npm
      - run: npm install
      - name: Run sh
        run: ./.github/test.sh